/**
 * The OpenRouter provider and default model slug live in `@repo/ai` so the
 * agents here and the web app's chat route share one config.
 */
export { DEFAULT_MODEL_ID, openrouter } from "@repo/ai";
